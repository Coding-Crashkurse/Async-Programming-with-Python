[project]
name = "asynprogramming"
version = "0.1.0"
description = "Asyncio Jupyter course"
requires-python = ">=3.12"
dependencies = [
    "anyio>=4.13.0",
    "jupyter>=1.1.1"
]

[tool.uv]
package = false
